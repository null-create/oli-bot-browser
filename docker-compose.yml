services:
  web:
    build: .
    ports:
      - "8080:80"
    environment:
      BACKEND_HOST: host.docker.internal
      BACKEND_PORT: "9734"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped